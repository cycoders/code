<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Coverage Merger Report</title>
    <style>
        body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; margin: 40px; }
        h1 { color: #333; }
        table { border-collapse: collapse; width: 100%; margin-top: 20px; }
        th, td { border: 1px solid #ddd; padding: 12px; text-align: left; }
        th { background-color: #f4f4f4; font-weight: bold; }
        tr:nth-child(even) { background-color: #f9f9f9; }
        .good { background-color: #d4edda !important; color: #155724; }
        .warn { background-color: #fff3cd !important; color: #856404; }
        .bad { background-color: #f8d7da !important; color: #721c24; }
        .delta-pos { color: #28a745; font-weight: bold; }
        .delta-neg { color: #dc3545; font-weight: bold; }
        .summary { background: #e7f3ff; padding: 20px; border-radius: 8px; margin-bottom: 20px; }
    </style>
</head>
<body>
    <h1>📊 Coverage Merger Report</h1>
    <div class="summary">
        <strong>Total Files:</strong> {{ total_files }} |
        <strong>Avg Line Coverage:</strong> {{ "%.1f"|format(avg_line_pct) }}%
        {% if prev_stats %}
        | <strong>Compared to baseline</strong>{% endif %}
    </div>
    <table>
        <thead>
            <tr>
                <th>File</th>
                <th>Lines %</th>
                <th>Branches %</th>
                <th>Δ Lines %</th>
                <th>Missed Lines</th>
            </tr>
        </thead>
        <tbody>
            {% for stat in stats %}
            {% set line_class = 'good' if stat.line_pct >= 90 else ('warn' if stat.line_pct >= 80 else 'bad') %}
            {% set delta = prev_stats.get(stat.file, 0) %}
            {% set delta_val = stat.line_pct - delta %}
            {% set delta_class = 'delta-pos' if delta_val >= 0 else 'delta-neg' %}
            <tr class="{{ line_class }}">
                <td>{{ stat.file }}</td>
                <td>{{ "%.1f"|format(stat.line_pct) }}%</td>
                <td>{% if stat.branch_pct %}{{ "%.1f"|format(stat.branch_pct) }}%{% else %}-{% endif %}</td>
                <td class="{{ delta_class }}">{{ "%.1f"|format(delta_val) if prev_stats else '-' }}%</td>
                <td>{{ stat.missed_lines }}</td>
            </tr>
            {% endfor %}
        </tbody>
    </table>
</body>
</html>
