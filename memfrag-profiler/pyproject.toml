[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "memfrag-profiler"
version = "0.1.0"
description = "Python memory fragmentation profiler"
readme = "README.md"
license = {text = "MIT"}
authors = [{name = "Arya Sianati"}]
dependencies = ["typer>=0.9", "rich>=13", "psutil>=5.9"]
requires-python = ">=3.11"