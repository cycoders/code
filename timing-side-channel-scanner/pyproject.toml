[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "timing-side-channel-scanner"
version = "0.1.0"
description = "Timing side-channel vulnerability scanner"
readme = "README.md"
license = {text = "MIT"}
authors = [{name = "Arya Sianati"}]
requires-python = ">=3.11"