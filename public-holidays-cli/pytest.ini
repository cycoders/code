[tool:pytest]
addopts = -ra -q --strict-markers
minversion = 6.0
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

[tool:pytest.ini_options]
markers =
    slow: marks tests as slow
    api: marks real API tests (avoid in CI)