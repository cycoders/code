[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "config-consistency-checker"
version = "0.1.0"
description = "Detect config drift and enforce policies"
readme = "README.md"
license = {text = "MIT"}
dependencies = ["typer>=0.9", "rich>=13", "pyyaml", "tomli", "jsonschema"]
